import React from 'react';
import { withRouter } from 'react-router-dom';

const Articles = () => (
  // <BasicLayout />
  <div>文章信息</div>
);

// Articles.propTypes = {
//   i: PropTypes.array,
// }

export default withRouter(Articles);
